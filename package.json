{
  "name": "todo-list-app",
  "version": "1.0.0",
  "description": "Simple To-Do List Application",
  "scripts": {
    "start": "http-server -p 5173",
    "dev": "http-server -p 5173",
    "build": "echo 'No build step required'",
    "cy:run": "cypress run",
    "cy:open": "cypress open",
    "test": "npm run cy:run"
  },
  "dependencies": {
    "mydevcave-hwv-project-setup": "^1.1.1"
  },
  "devDependencies": {
    "cypress": "^13.3.0",
    "http-server": "^14.1.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "wait-on": "^7.0.1"
  }
}
